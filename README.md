# House-Price-Predictor
Machine learning–based house price prediction system built in Python using a Random Forest model. It estimates prices from user inputs, applies preprocessing for accuracy, and stores prediction history in a SQLite database through a simple CLI interface.
